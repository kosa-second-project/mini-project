package kr.or.bit.service;

public class EmpService {

    public boolean validatePassword(int empno, String password) {
        // TODO EMP 담당 구현 연결 후 실제 사원 비밀번호 검증으로 교체한다.
        return "1234".equals(password);
    }
}
